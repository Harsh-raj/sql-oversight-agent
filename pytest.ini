[pytest]
pythonpath = .
markers =
    integration: requires a running Ollama instance and seeded SQLite DB
    stub_integration: full graph against a real HTTP client + fake Ollama-compatible server; no external dependency, runs in CI
