# sql-oversight-agent
Designed and built a LangGraph-Orchestrated agent that decomposes natural-language business questions into independent sub-queries, generates SQL against a locally hosted model and validates results before answering - with zero hallucination tolerance as explicit design constraint.
